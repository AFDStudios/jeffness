jQuery(document).ready(function($) {
	$('.cnr-slider-wrapper').slick({
		dots: true,
		infinite: true,
		slidesToShow: 1,
		slidesToScroll: 1,
		arrows: true,
		// variableWidth: true,
	});
}); /* end of as page load scripts */